#include <iostream> // For input and output operations

int main() {
    std::cout << "Learning Coding"; // 'cout' should be 'std::cout' with the 'std' namespace
    std::cin.get(); // 'getch()' is not available in standard C++, so 'cin.get()' can be used to wait for a keypress
    return 0;
}
